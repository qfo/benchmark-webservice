Logger := proc(msg ; (level='INFO'):{'INFO','CRITICAL','DEBUG'})
    fn := sprintf('/proc/%d/fd/2', getpid());
    OpenWriting(fn);
    printf('%s [%s]: %a\n', date(), level, msg);
    OpenWriting(previous);
end:


